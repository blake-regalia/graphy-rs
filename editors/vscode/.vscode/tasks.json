{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build server",
      "type": "shell",
      "command": "cargo build -p graphy-lsp --release && cd editors/vscode && mkdir -p server && ln -sf ../../../target/release/graphy-lsp server/graphy-lsp",
      "options": {
        "cwd": "${workspaceFolder}/../.."
      },
      "problemMatcher": [
        "$rustc"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "presentation": {
        "reveal": "silent",
        "clear": true
      }
    }
  ]
}
