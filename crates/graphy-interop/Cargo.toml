[package]
name = "graphy-interop"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Interchange codecs for graphy: RDF/XML and JSON-LD parse + serialize"

[dependencies]
graphy-core = { path = "../graphy-core" }
serde_json = "1"
getrandom.workspace = true

[dev-dependencies]
graphy-turtle = { path = "../graphy-turtle" }
